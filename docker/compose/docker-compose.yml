version: "3.9"

services:

  postgres:
    image: postgres:16

    container_name: aihf-postgres

    restart: unless-stopped

    env_file:
      - ../shared/.env.example

    ports:
      - "5432:5432"

    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:

  postgres_data: